package oldloc.player.heroes;

import oldloc.player.Hero;
import oldloc.player.User;
import org.jetbrains.annotations.NotNull;

public class Recruit extends Hero {
    public Recruit(@NotNull final User user) {
        super(user);
    }
}
